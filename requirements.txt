# PDF parsing dependencies
docling[pdf,vision]>=2.0.0
pillow>=10.0.0
marker-pdf>=1.0.0
pdfplumber>=0.10.0  # For text-based PDFs with complex tables
pymupdf>=1.24.0  # For layout detection and table region extraction

# Visual table structure detection (optional / comprehensive mode)
opencv-python-headless>=4.8.0  # For drawing grid lines
pdf2image>=1.16.0  # For PDF to image conversion (requires Poppler)
paddleocr==3.3.2  # PaddleOCR v3 (PPStructureV3)
paddlex[ocr]==3.3.11  # Required extras for PPStructureV3 table recognition

# CLI
click>=8.1.0

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0

# Type checking
mypy>=1.8.0

